USER (USERNAME, _ID_ , PASSWORD)

CONVERSATION ( _ID_ )

POST ( _ID_, _CONVERSATION_, TEXT, FATHER, FIRST_RESPONSE, SECOND_RESPOSE )
  FK: CONVERSATION   references CONVERSATION
  FK: FATHER         references POST
  FK: FIRST_RESPONSE references POST
  FK: SECOND_RESPOSE references POST
  
AUTHOR ( _USER_, _POST_ )
  FK: USER references USER 
  FK: POST references POST

ARCHIVE ( _USER_, _CONVERSATION_, PROPRIETARIO)
  FK: USER         references USER
  FK: CONVERSATION references CONVERSATION
